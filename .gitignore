# Binaries
/juridical
/dist/
/build/
bin/

# Compiled / native
*.exe
*.dll
*.so
*.dylib

# Test & coverage artifacts
*.out
*.test
coverage.txt
coverage.html
cover.out

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local runtime state
/data/
/config/
*.local.yaml
*.local.yml

# Environment & secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
*_rsa
*.pat
secrets/

# Local PII denylist (external, never committed)
.pii_denylist
PII_DENYLIST

# Release / signing scratch
*.sbom.json
*.sig
*.pem.pub

# Native fuzz corpus (advisory `make fuzz`) — generated locally by go test
# -fuzz; interesting-but-non-crashing inputs live in the build cache, not
# here. Only a genuine crasher regression file should ever be committed
# under testdata/fuzz/, and only deliberately (git add -f).
testdata/fuzz/
